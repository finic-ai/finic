import { ProviderConfiguration } from '../types';
export declare const preset: {
    readonly isOnline: () => boolean;
    readonly isVisible: () => boolean;
};
export declare const defaultConfigOptions: ProviderConfiguration;
