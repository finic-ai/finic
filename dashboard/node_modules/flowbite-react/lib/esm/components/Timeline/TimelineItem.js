import { jsx as _jsx } from "react/jsx-runtime";
import classNames from 'classnames';
import { useTheme } from '../Flowbite';
import { useTimelineContext } from './TimelineContext';
export const TimelineItem = ({ children, className, ...props }) => {
    const theme = useTheme().theme.timeline.item;
    const { horizontal } = useTimelineContext();
    return (_jsx("li", { "data-testid": "timeline-item", className: classNames(horizontal && theme.base.horizontal, !horizontal && theme.base.vertical, className), ...props, children: children }));
};
