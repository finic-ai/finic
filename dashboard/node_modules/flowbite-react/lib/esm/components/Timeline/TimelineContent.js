import { jsx as _jsx } from "react/jsx-runtime";
import classNames from 'classnames';
import { useTheme } from '../Flowbite';
import { useTimelineContext } from './TimelineContext';
export const TimelineContent = ({ children, className, ...props }) => {
    const theme = useTheme().theme.timeline.item.content;
    const { horizontal } = useTimelineContext();
    return (_jsx("div", { "data-testid": "timeline-content", className: classNames(horizontal && theme.base, className), ...props, children: children }));
};
